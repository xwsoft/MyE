<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mye.gmobile"
    android:versionCode="8"
    android:versionName="2.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="15" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar" >
        <activity
            android:name="com.mye.gmobile.activity.SplashActivity"
            android:launchMode="singleTask"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.GuideActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.HouseListActivity" />
        <activity android:name=".activity.ThermostatActivity" />
        <activity android:name=".activity.DialogEditTempActivity" android:theme="@style/MyDialogStyle"/>
        <activity android:name=".activity.DialogEditHoldActivity" android:theme="@style/MyDialogStyle"/>
        <activity android:name=".activity.DialogWeeklyModeEditActivity" android:theme="@style/MyDialogStyle"/>
        <activity android:name=".activity.DialogVacationMenuActivity" android:theme="@style/dialog"/>
        <activity android:name=".activity.VacationEditActivity"/>
        
        <activity android:name=".activity.DialogVacationTypeActivity" android:theme="@style/MyDialogStyle"/>
        <activity android:name=".activity.DialogDateActivity" android:theme="@style/MyDialogStyle"/>
        <activity android:name=".activity.DialogTimeActivity" android:theme="@style/MyDialogStyle"/>
        <activity android:name=".activity.DialogTemperatureActivity" android:theme="@style/MyDialogStyle"/>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" >
    </uses-permission>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" >
    </uses-permission>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" >
    </uses-permission>
    <!-- 在sd卡中创建与删除文件权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <!-- 向SD卡写入数据权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

</manifest>